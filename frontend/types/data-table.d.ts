import { LucideIcon } from "lucide-react"

export type Variants = {
    value: string // este value debe ser igual a la variante que se valla  a usar.  
    label: string
    icon?: LucideIcon
}